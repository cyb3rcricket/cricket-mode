---
name: drift
description: "Use only when the user invokes /drift. Follow the Cricket contract section headed /drift. Do not apply this because the task merely resembles drift."
---

# /drift

Antigravity wiring for Cricket `drift`. This file does not define the behavior.

Cricket adapter: antigravity

Antigravity documents this skill as the `/drift` slash command. It also lists the skill for autonomous activation. The documented frontmatter has no switch that turns that activation off.

Read `../cricket-contract/COMMANDS.md`. Follow only the section headed `/drift` and the section headed `Cross-command invariants`. If that file is missing or unreadable, stop and say the Cricket contract is not installed.

Do not invent a different purpose, scope boundary, evidence requirement, or result shape.
