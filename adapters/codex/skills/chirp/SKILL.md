---
name: chirp
description: "Cricket chirp. Explicit $chirp only. Follow the /chirp section of the shared Cricket contract. Do not apply this unless the user invokes $chirp."
---

# $chirp

Codex wiring for Cricket `chirp`. This file does not define the behavior.

Codex invokes this skill as `$chirp` or from the `/skills` picker. It does not register a `/chirp` slash command.

Read `../cricket-contract/COMMANDS.md`. Follow only the section headed `/chirp` and the section headed `Cross-command invariants`. If that file is missing or unreadable, stop and say the Cricket contract is not installed.

Do not invent a different purpose, scope boundary, evidence requirement, or result shape.
