---
name: scrub
description: "Cricket scrub. Explicit $scrub only. Follow the /scrub section of the shared Cricket contract. Do not apply this unless the user invokes $scrub."
---

# $scrub

Codex wiring for Cricket `scrub`. This file does not define the behavior.

Codex invokes this skill as `$scrub` or from the `/skills` picker. It does not register a `/scrub` slash command.

Read `../cricket-contract/COMMANDS.md`. Follow only the section headed `/scrub` and the section headed `Cross-command invariants`. If that file is missing or unreadable, stop and say the Cricket contract is not installed.

Do not invent a different purpose, scope boundary, evidence requirement, or result shape.
